import React from 'react'

const Reducer = ( state, action) => {
  
    switch (action.type) {
        case "add":
         return{
            
         }
    
        default:
            break;
    }

}

export default Reducer